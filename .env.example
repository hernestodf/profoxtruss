# BASE_URL é opcional — se omitido, o sistema detecta automaticamente.
# Defina apenas em produção para garantir o domínio certo.
# BASE_URL=https://meusite.com/subpasta

APP_ENV=development
APP_DEBUG=true

DB_HOST=localhost
DB_NAME=projeto_db
DB_USER=root
DB_PASS=

SESSION_LIFETIME=7200


